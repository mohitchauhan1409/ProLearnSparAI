<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme — dark window background prevents any white flash on cold start -->
    <style name="Theme.ProLearnSparAI" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/splash_bg</item>
    </style>

    <!-- Android 12+ SplashScreen API theme — shows the logo the instant the user taps the icon -->
    <style name="Theme.ProLearnSparAI.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_bg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">400</item>
        <item name="postSplashScreenTheme">@style/Theme.ProLearnSparAI</item>
    </style>

</resources>
